apiVersion: v1
kind: Service
metadata:
  name: MyEksctlTask
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: MyEksctlTask
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: MyEksctlTask
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 2
      maxSurge: 2
  selector:
    matchLabels:
      app: MyEksctlTask
  template:
    metadata:
      labels:
        app: MyEksctlTask
    spec:
      containers:
        - name: MyEksctlTask
          image: CONTAINER_IMAGE
          securityContext:
            privileged: false
            readOnlyRootFilesystem: false
            allowPrivilegeEscalation: false
          ports:
            - containerPort: 8080
